syntax = "proto3";
option go_package = "example.com/grpc-dir/proto;proto";

package fileservice;

service file_service {
  rpc list_dir(path) returns (dir);
}

message path {
  string path = 1;
}

message dir {
  repeated string filenames = 1;
}
