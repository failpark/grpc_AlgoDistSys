syntax = "proto3";

package fileservice;

option go_package = "grpc-seminar/proto";

service file_service {
  rpc list_dir(path) returns (dir);
}

message path {
  string path = 1;
}

message dir {
  repeated string filenames = 1;
}
